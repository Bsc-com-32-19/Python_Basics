def square(number):
    # if we can remove the key word return and print the number it will
    # display none cos that's the default value in ...
    # function if we did not add key word return
    return number * number


result = square(3)
print(result)

# instead of writing two line we can put it in a line
print(square(3))
